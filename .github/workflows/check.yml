name: ISPARK Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: ISPARK bağlantı testi
        run: |
          echo "ISPARK test ediliyor..."
          curl -I --connect-timeout 20 --max-time 30 https://ispark.istanbul
